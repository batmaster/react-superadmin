import React from 'react';

// Export React for JSX usage
export { React };

// Export default component (required by Docusaurus)
export default function MDXComponents(props: any) {
  return <div {...props} />;
}
